#!/usr/bin/env node
/* patch-farmghost-v134.js — v134.12: the player's fields stop aiming themselves.
 *
 * John, after a playtest: "not a fan of how the farms rotate around town center, storage pits etc
 * can we revert them back to where I can manually rotate them if I want to?"
 *
 * v134.8 gave the placement ghost the AI's own facing rule, recomputed every frame, and argued for
 * it like this: "a field he plants beside the AI's should not be the one facing the wrong way."
 * That reasoning is about a TOWN, and the thing it was applied to is a man lining up a building
 * with a mouse. A ghost that re-aims itself under the cursor is not a convenience however good its
 * arithmetic, and R-to-override does not rescue it: you have to fight it before you can use it.
 *
 * SO THE PLAYER HALF GOES AND THE AI HALF STAYS — John's call, asked and answered, and it is also
 * the measured one. Reverting the AI would bring back what v134.8 fixed: barns lapping the Town
 * Center and storage-pit boxes 104 times a campaign, up to 0.47 units deep. A bot places
 * unattended and cannot be asked to press R. A player can.
 *
 * WHAT STAYS: the rot PLUMBING. placing.rot still reaches makeBuilding and NET.guestAct, which is
 * what makes R work for a farm at all, and for a guest. That was v134.8's other half and it was
 * never the part anybody objected to.
 *
 * WHAT GOES WITH IT: placing.rotManual. It existed for exactly one reason — to stop the per-frame
 * auto-facing snapping the ghost back the instant the key was released. With nothing to hold off,
 * a flag that guards nothing is a trap for whoever reads this next, so it goes and the gate
 * asserts its absence.
 */
const fs=require("fs"),path=require("path");
const R=path.join(__dirname,"..");
const I=path.join(R,"js","06-input.js"), S=path.join(R,"tools","smoketest.js");
let i=fs.readFileSync(I,"utf8"), s=fs.readFileSync(S,"utf8");
let sw=fs.readFileSync(path.join(R,"sw.js"),"utf8"), h=fs.readFileSync(path.join(R,"index.html"),"utf8");
let failed=[];
const sub=(name,src,from,to)=>{
  const n=src.split(from).length-1;
  if(n!==1){failed.push(name+" (matched "+n+" times, need exactly 1)");return src;}
  return src.split(from).join(to);
};

i=sub("the ghost stops aiming itself",i,"  // stage-0 marker and every ordinary building: the classic follow\n  // v134.8 \u2026and a FARM turns its barn away from whatever it is ringing, exactly as the AI's do.\n  // The ring binds the player identically \u2014 validFor is the one choke point \u2014 so his barns lap the\n  // same Town Center box, and a field he plants beside the AI's should not be the one facing the\n  // wrong way. Recomputed per frame because the anchor changes as he walks around the town; R\n  // stops it (rotManual), and the value lands in placing.rot so the commit and the guest's\n  // build request both carry it without a second code path.\n  if(placing.type===\"farm\"&&!placing.rotManual)placing.rot=farmFacing(MYTEAM,a.x,a.z);\n  ghost.position.set(a.x,terrainHeight(a.x,a.z),a.z);","  // stage-0 marker and every ordinary building: the classic follow\n  // v134.12 THE PLAYER'S FIELDS DO NOT TURN THEMSELVES. v134.8 gave the ghost the AI's own facing\n  // rule, recomputed every frame, on the reasoning that \"a field he plants beside the AI's should\n  // not be the one facing the wrong way\". John playtested it: \"not a fan of how the farms rotate\n  // around town center, storage pits etc \u2014 can we revert them back to where I can manually rotate\n  // them if I want to?\" A convenience that re-aims a building under the cursor while you are\n  // trying to line it up is not a convenience, whatever it is optimising.\n  // So the ghost places square-on and R turns it, exactly as every other building has always\n  // worked. THE AI KEEPS ITS FACING \u2014 John's call, and the measured one: without it the marshals'\n  // barns lapped the Town Center and pit boxes 104 times a campaign, up to 0.47 deep. What was\n  // wrong was applying a rule about a bot's unattended placement to a man with a mouse.\n  // The rot PLUMBING stays: placing.rot still reaches makeBuilding and guestAct, which is what\n  // makes R work for a farm at all, and for a guest.\n  ghost.position.set(a.x,terrainHeight(a.x,a.z),a.z);");
i=sub("…and the flag that guarded it goes with it",i,"  if(placing&&k===\"r\"){ // rotate the foundation\n    placing.rot=((placing.rot||0)+Math.PI/4)%(Math.PI*2);\n    // v134.8: and from here on this placement is HIS. A farm's ghost turns its barn outward on\n    // every frame (updateGhostFollow), which without this flag would snap back the instant he let\n    // go of the key \u2014 an auto-orientation that cannot be overridden is a bug, not a convenience.\n    placing.rotManual=true;\n    if(ghost)ghost.rotation.y=placing.rot;\n    return;\n  }","  if(placing&&k===\"r\"){ // rotate the foundation\n    placing.rot=((placing.rot||0)+Math.PI/4)%(Math.PI*2);\n    // v134.12 rotManual is GONE with the thing it guarded. It existed only to stop the per-frame\n    // auto-facing snapping the ghost back the instant the key was released; with no auto-facing\n    // there is nothing to hold off, and a flag that guards nothing is a trap for whoever reads it\n    // next. Every placement is manual now, which is what it was before v134.8.\n    if(ghost)ghost.rotation.y=placing.rot;\n    return;\n  }");
s=sub("the gate asserts the ABSENCE, and that the AI keeps its own",s,"    // In updateGhostFollow, so it re-aims as he walks around the town; guarded by rotManual, so R\n    // takes it back; and R must SET that flag or the ghost snaps home the moment he lets go.\n    const inGhost=inp.indexOf('if(placing.type===\"farm\"&&!placing.rotManual)placing.rot=farmFacing(MYTEAM,')>=0;\n    const rKey=/placing\\.rot=\\(\\(placing\\.rot\\|\\|0\\)\\+Math\\.PI\\/4\\)[\\s\\S]{0,400}?placing\\.rotManual=true/.test(inp);\n    // \u2026and the value has to land in placing.rot rather than straight on the mesh, because that is\n    // the field the commit and the guest's build request both read (06-input.js:850/856).\n    const commits=inp.indexOf('makeBuilding(MYTEAM,placing.type,x,z,false,placing.rot||0)')>=0;\n    const guest=inp.indexOf('NET.guestAct({act:\"build\",type:placing.type,x:x,z:z,rot:placing.rot||0})')>=0;\n    check(\"v134.8 ghost: a player's farm turns itself the same way the AI's do \u2014 in \"+\n      \"updateGhostFollow (\"+inGhost+\"), overridable with R (\"+rKey+\"), and carried on placing.rot \"+\n      \"so the host's own commit (\"+commits+\") and a guest's build request (\"+guest+\") both ship \"+\n      \"the rotation without a second code path\",\n      inGhost&&rKey&&commits&&guest);","    // \u26a0 COMMENTS STRIPPED FIRST, and this gate is the reason the rule exists. Two of the reads\n    // below ask whether a form is GONE, and the comment that explains WHY it is gone names it \u2014\n    // so the first cut went red on its own explanation, exactly as the v134.9 map gate did. A\n    // line-comment strip is the honest instrument: a read a comment can satisfy is a read a\n    // comment can also defeat. (Checked: no \"//\" inside a string literal on any line these\n    // searches touch, in either file.)\n    const _strip=(t)=>t.split(String.fromCharCode(10))\n      .map(l=>{const c=l.indexOf(\"//\");return c<0?l:l.slice(0,c);}).join(String.fromCharCode(10));\n    // \u2026and 07-ai.js is read HERE rather than borrowed from the block above: `ai4` is scoped to\n    // the v134.10 garrison gate, and reaching for it crashed the whole suite on load.\n    const aiSrc=fs2.readFileSync(path2.join(__dirname,\"..\",\"js\",\"07-ai.js\"),\"utf8\");\n    const inpC=_strip(inp), aiC=_strip(aiSrc);\n    const RKEY_RE=new RegExp(\"if\\\\(placing&&k===.r.\\\\)\\\\{[\\\\s\\\\S]{0,600}?placing\\\\.rot=\\\\(\\\\(placing\\\\.rot\\\\|\\\\|0\\\\)\\\\+Math\\\\.PI/4\\\\)\");\n    // v134.12 THE GHOST DOES NOT AIM ITSELF, and this gate now says so from the other side. It\n    // asserted the auto-facing was PRESENT; the claim is that it is ABSENT, that R still turns the\n    // foundation, and that the rot still reaches the two places a rotation has to reach.\n    const inGhost=inpC.indexOf('placing.rot=farmFacing(MYTEAM,')>=0;      // must be GONE\n    const dead=inpC.indexOf('rotManual')>=0;                              // \u2026and so must its flag\n    const rKey=RKEY_RE.test(inpC);\n    // \u2026and the value has to land in placing.rot rather than straight on the mesh, because that is\n    // the field the commit and the guest's build request both read (06-input.js:850/856) \u2014 which is\n    // what makes R work for a farm at all, and for a guest. That plumbing is v134.8's and it stays.\n    const commits=inpC.indexOf('makeBuilding(MYTEAM,placing.type,x,z,false,placing.rot||0)')>=0;\n    const guest=inpC.indexOf('NET.guestAct({act:\"build\",type:placing.type,x:x,z:z,rot:placing.rot||0})')>=0;\n    // \u2026and the AI's own farms still turn, because that is the half that was fixing a real overlap.\n    const aiTurns=aiC.indexOf('makeBuilding(team,\"farm\",fs.x,fs.z,false,farmFacing(team,fs.x,fs.z))')>=0;\n    check(\"v134.12 ghost: a player's farm does NOT aim itself \u2014 the per-frame auto-facing is gone \"+\n      \"(\"+(!inGhost)+\") and so is the rotManual flag that only existed to hold it off (\"+(!dead)+\n      \"). R turns the foundation (\"+rKey+\") and the value still travels on placing.rot, so the \"+\n      \"host's commit (\"+commits+\") and a guest's build request (\"+guest+\") both carry it. THE AI \"+\n      \"STILL TURNS ITS OWN (\"+aiTurns+\"): unattended placement laps the Town Center box 104 times \"+\n      \"a campaign without it. John, playtesting: \\\"not a fan of how the farms rotate ... can we \"+\n      \"revert them back to where I can manually rotate them if I want to\\\"\",\n      !inGhost&&!dead&&rKey&&commits&&guest&&aiTurns);");
sw=sub("sw VERSION",sw,"const VERSION=\"v134.11\";","const VERSION=\"v134.12\";");
h=sub("index verstamp",h,"<p class=\"verstamp\">v134.11 \u2014 THE WALL THAT WAS NOT THERE</p>","<p class=\"verstamp\">v134.12 \u2014 THE FIELDS STOP AIMING THEMSELVES</p>");
if(failed.length){
  console.error("PATCH ABORTED — anchors did not match exactly once:");
  for(const f of failed)console.error("  · "+f);
  process.exit(1);
}
fs.writeFileSync(I,i); fs.writeFileSync(S,s);
fs.writeFileSync(path.join(R,"sw.js"),sw); fs.writeFileSync(path.join(R,"index.html"),h);
console.log("patch-farmghost-v134: OK");
